import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.mathapp',
  appName: 'mathapp',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
